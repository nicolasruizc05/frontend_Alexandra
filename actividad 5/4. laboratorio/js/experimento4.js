// Experimento 4 
// Seleccionar elementos utilizando diferentes tipos de selectores
let title = document.querySelectorAll("#titulo")
let clase = document.querySelectorAll(".favorito")
let etiqueta = document.querySelectorAll("img")
console.log(title)
console.log(clase)
console.log(etiqueta)
